# Tasks for Global Project Filtering (#6)

- [x] Update `MainLayoutContext` types with `activeProjectId` and `setActiveProjectId` state in `MainLayout.tsx`.
- [x] Add `activeProjectId` and `setActiveProjectId` props to `Sidebar.tsx`.
- [x] Add conditional styling for active state on `li.projectItem` strings (`clsx(styles.projectItem, activeProjectId === project.id && styles.activeProject)`).
- [x] Implement `onClick` on project items: calling `setActiveProjectId(project.id)`. Add logic to clear the filter if clicked again.
- [x] Add an `onClick` exclusively to the `Inbox` navigation link that implicitly clears the active project filter, per decisions in spec.md.
- [x] Update `useTasks(viewName, filterProjectId)` signature in `src/features/tasks/hooks/useTasks.ts`.
- [x] Append a single `where('projectId', '==', filterProjectId)` to the Firestore queries in `useTasks` if `filterProjectId` is present.
- [x] Prevent fetching/merging Google Tasks if `filterProjectId` is populated.
- [x] Update `Inbox.tsx` through `Logbook.tsx` to read `activeProjectId` via `useOutletContext<MainLayoutContext>()` and pass it into their `useTasks(...)` hook usages.
- [x] Test the filtering behavior across Today, Anytime, Upcoming, Someday, and Logbook.
- [ ] Build required Firestore composite indexes generated by the browser console errors, and copy the new definitions into `firestore.indexes.json`.
- [x] Commit these changes and create/update PR for #6.
